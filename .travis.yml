language: node_js
node_js:
  - "node"
  - "lts/*"
  - "8"
before_install:
  - npm install graphql@^${GRAPHQL_VERSION}
before_script:
  - npm run lint
  - npm run flow

env:
  - GRAPHQL_VERSION=0.13.0
